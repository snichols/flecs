# Queries

<!-- TODO: port from /work/agents/claude/projects/SanderMertens/flecs/docs/Queries.md (Phase 14.2) -->

This manual covers the full query API: AND / NOT / Optional / OR terms, cached vs. uncached queries, traversal modifiers (Up, SelfUp, Cascade), inheritable components, change detection, and query introspection.

See the [Quickstart](Quickstart.md) for a hands-on introduction.
