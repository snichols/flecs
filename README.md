# flecs (Go port)

A Go port of [flecs](https://github.com/SanderMertens/flecs), a fast and lightweight Entity Component System (ECS) library.

The port is in progress. This repository mirrors the upstream bit layout and semantics of flecs exactly.
